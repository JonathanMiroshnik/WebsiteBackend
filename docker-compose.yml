version: '3.8'

services:
  backend:
    build: .
    container_name: websitebackend
    restart: unless-stopped
    ports:
      - "5000:5000"
    env_file:
      - .env
    networks:
      - devops_shared

networks:
  devops_shared:
    external: true
